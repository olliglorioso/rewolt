import { AppBar, Toolbar } from "@mui/material";
import React from "react";

const MenuBar = () => {
  return (
    <AppBar>
      <Toolbar>
        Hi
      </Toolbar>
    </AppBar>
  )
}

export default MenuBar